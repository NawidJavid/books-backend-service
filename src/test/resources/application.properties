# Test configuration - minimal settings for unit tests
# The actual database is mocked in tests, so these are placeholder values

books.db.type=mysql
books.db.url=jdbc:mysql://localhost:3306/testdb?user=test&password=test
